<?php

use Helper\Route\Route;
use Codeception\Test\Unit;

class RouteTest extends Unit
{
    /**
     * @var \UnitTester
     */
    protected $tester;
    
    protected function _before()
    {
    }

    protected function _after()
    {
    }

    // tests
    public function testSomeFeature()
    {
//        $route = new Route([]);
//        $this->tester->assertSame('', $route->getMethod());
//        $this->tester->assertSame('', $route->getController());
//        $this->tester->assertSame('', $route->getPattern());
    }
}